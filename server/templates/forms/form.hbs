<form id="{{formId}}" name="{{formName}}" class="{{form.class}}" {{{controllAttrs}}}action="{{form.action}}" method="{{form.method}}">
<fieldset><legend>{{t form 'title'}}</legend>{{{fields}}}</fieldset>
</form>